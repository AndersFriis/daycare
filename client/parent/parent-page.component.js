import template from './parent-page.html';

import ParentPageController from './parent-page.controller';

const parentPageComponent = {
    template,
    controller: ParentPageController,
    controllerAs: 'parentPageCtrl',
};

export default parentPageComponent;